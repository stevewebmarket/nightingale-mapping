# NMB3 Decisions Log
#
# Each entry is a single Steve decision, timestamped (UTC ISO 8601),
# quoting Steve's exact wording verbatim.  Appended only by the
# autonomous loop on receipt of an explicit Steve string.  Entries
# are append-only; they are never edited or removed.
#
# Format:
#   [<UTC timestamp>] Steve, via <channel>, replying to <prior artifact>:
#     "<exact wording>"
#     Effect on roadmap (per nmb3/nmb3_objective_map.md):
#       - <objective>: <state transition or authorisation>
#       - ...
#
# ----------------------------------------------------------------

[2026-04-25T16:34:20Z] Steve, via chat reply to the O001 autonomous-portion report (canonical 42a113b):
  "Close M3.1, narrow reading as above. Authorise O002: per-case
   decomposition of the mid-range onset_delta plateau."

  Effect on roadmap (per nmb3/nmb3_objective_map.md, refinement r1):
    - O001 (M3.1 closure decision): CLOSED (Steve-approved).
      Closure on the "narrow reading" defined in
      nmb3/nmb3_reports/m3_1_closure_recommendation.md, section
      "What 'M3.1 Closed' Would Mean (Narrow Reading, Recommended)".
      Authorises only the four claims listed there; explicitly does
      NOT authorise M4 work, any funding claim, the perceptual-
      validity claim, generalisation beyond the canonical 6-case
      sample set, generalisation to parameters other than
      onset_delta, or any policy-file edit.
    - O002 (M3.1 per-case mid-range decomposition): authorised by
      this reply, satisfying O002's "starting without Steve
      authorisation in O001's reply" forbidden-action.  Status moves
      from PROPOSED to STEVE-APPROVED (NOT STARTED).  The autonomous
      loop will not begin O002 in this session, per the loop policy
      "do not begin the next objective" rule and per Steve's
      standing "do not execute the whole preferred path yet"
      constraint.
    - O003 (M3.1 cross-corpus probe): NOT authorised by this reply.
      Remains PROPOSED, off-path-but-available.
    - M3.1 -> M4 transition: NOT authorised.  Option 4 of the
      closure recommendation was not chosen.
    - Funding claim: NOT authorised.  No funder-facing claim may be
      made on the strength of this closure.
    - Policy file edit: NOT authorised.  No edit to
      nmb3/nmb3_decision_policy.md or
      nmb3/nmb3_autonomous_loop_policy.md is implied or permitted.
    - MILESTONES.md: NOT edited.  The locked record is unchanged.

[2026-04-25T16:46:06Z] Steve, via chat reply to the O001 closure report (canonical 1f01854):
  "Execute O002"

  Effect on roadmap (per nmb3/nmb3_objective_map.md):
    - O002 (M3.1 per-case mid-range decomposition): execution
      authorised.  Status moves from STEVE-APPROVED (NOT STARTED)
      to IN PROGRESS.  The autonomous loop will execute the
      bounded shape exactly as specified in O002's
      "Allowed autonomous actions" field: write Block 005 plan
      varying onset_delta in {0.10, 0.15, 0.20}; smoke-test;
      dispatch via the CI workflow rewire pattern proven in
      Blocks 003 and 004; produce per-case (6 cases x 3 deltas)
      within/total counts; verify determinism by running each
      delta twice; flag any case whose within count moves by >= 1
      across the 3 deltas.
    - O003, O004, every later objective: NOT authorised by this
      reply.  Remain at their current status (PROPOSED for O003;
      PROPOSED for O004 and beyond).
    - M3.1 closure: NOT re-opened.  M3.1 remains CLOSED on the
      narrow reading per the 2026-04-25T16:34:20Z entry.  Block
      005 is a follow-on within the closed milestone, resolving
      the plateau pattern without changing closure status.
    - Funding claim: NOT authorised.  Policy file edit: NOT
      authorised.  MILESTONES.md edit: NOT authorised.

[2026-04-25T17:22:10Z] Loop errata recorded by interpreter (no Steve action required):
  In the report closing O004, the loop incorrectly described
  the canonical objective O020 as "M5.6 lock re-validation".
  The on-canonical O020 is in fact "M5.4 re-document the
  pitch-window NEGATIVE result"; the M5.6 lock re-validation
  objectives are O022 and O023.  When Steve's reply
  "Execute O020" was received, the loop stopped before
  executing, surfaced the misdescription explicitly, and
  presented the actual P4 landscape with five clean string
  options.  No work was performed under the misdescribed
  authorisation.  This entry is informational only -- Steve's
  subsequent gating string ("Execute O023") was issued against
  the corrected landscape.

[2026-04-25T17:22:10Z] Steve, via chat reply to the corrected P4 landscape:
  "Execute O023"

  Effect on roadmap (per nmb3/nmb3_objective_map.md):
    - O023 (M5.6 fresh-clone reproducibility check, on preferred
      path): authorised to execute.  O023's Auto-execute
      permission was already YES under the Decision Policy
      validation/measurement-integrity criterion; Steve's string
      makes the execution explicit.
    - O023 is interchangeable with O012 as the creator of the
      shared no-cache reproducibility workflow at
      .github/workflows/nmb3_no_cache_repro.yml; O023 ran first,
      so O023 creates the file per the spec.
    - No other objective's status changes by this string.

  Loop interpretation:
    - Execute O023 per its spec (success criteria: bit-identical
      107/144; per-clip / per-transform reproduces to the note-
      flip; routing distribution reproduces to +/- 2 routing
      decisions; CI run id pinned by SHA in the interpreter
      output).
    - Per O023's Allowed actions clause, create the shared
      no-cache workflow yml at the spec'd path with the spec'd
      shape; dispatch with target=m5_6; record run id and SHA;
      write interpreter output.
    - Per O023's Forbidden actions clause, do NOT cache, do NOT
      cherry-pick runs, do NOT introduce any M5.6-specific
      logic into the shared workflow yml.
    - Per the Session Completion Rule, stop after success and
      propose the next objective without beginning it.

[2026-04-25T17:41:53Z] Steve, via chat reply to the O023 SUCCEEDED report:
  "Execute O024 (partial-scope closure)"

  Effect on roadmap (per nmb3/nmb3_objective_map.md):
    - O024 (P4 closure recommendation): authorised to execute
      in the partial-scope variant defined in the loop's prior
      proposal -- aggregate O023 alone, with O020 / O021 / O022
      explicitly deferred under the "Activation rule for off-path
      objectives" clause (which permits off-path objectives to
      remain deferred when an on-path objective produces evidence
      that does NOT point back at them, and O023 produced zero
      drift -- no regression to localise).
    - O024's Auto-execute permission was already YES under the
      Decision Policy validation/measurement-integrity criterion;
      Steve's string makes the partial-scope execution explicit
      and selects between the literal-spec reading ("aggregate
      all four") and the activation-rule reading ("aggregate the
      successful preferred-path objective alone, defer the others
      under the activation rule").  Steve chose the latter.
    - No other objective's status changes by this string.  In
      particular, O020 / O021 / O022 remain PROPOSED, not
      SUPERSEDED -- the partial-scope closure preserves the
      future-expansion path to a full-scope v2 recommendation
      aggregating all four.

  Loop interpretation:
    - Write nmb3/nmb3_reports/m5_phase_p4_closure_recommendation.md
      including the headline 107/144 = 0.7431 (required by O024's
      success criterion).
    - Aggregate O023's evidence in full (run ids, head SHA,
      per-cell breakdown, routing distribution) but mark O020 /
      O021 / O022 as deferred under the activation rule, not
      claimed.
    - Per O024's Forbidden actions clause, do NOT claim
      improvement on the M5.6 baseline.  No improvement is
      claimed; reproduction of the locked baseline is the entire
      content.
    - Mark the recommendation as a draft on canonical until
      Steve issues an explicit approval string ("Approve P4
      partial-scope closure" or equivalent).  The recommendation
      is not binding until that string lands.
    - Per the Session Completion Rule, stop after success and
      propose the next objective without beginning it.  This is
      loop 6 of 6 in the session budget; the next loop will
      require a fresh session.

[2026-04-25T17:59:01Z] Steve, via chat reply to the O024 SUCCEEDED report:
  "Approve P4 partial-scope closure"

  This is the binding approval string requested in section 7 of
  nmb3/nmb3_reports/m5_phase_p4_closure_recommendation.md.

  Effect on the recommendation:
    - Status of the recommendation file changes from "draft on
      canonical pending Steve approval" to "APPROVED by Steve at
      2026-04-25T17:59:01Z, binding for funding-package purposes
      on the partial-scope basis defined in section 2".
    - Section 1 of the recommendation file (file header) is
      updated to reflect the new status.
    - A new section 8 ("Approval record") is appended to the
      recommendation file, recording Steve's verbatim approval
      string, the timestamp, the channel, the binding effect,
      the preserved future-expansion path, and cross-references
      to the objective map, this decisions.log entry, the O023
      evidence file, and the shared workflow file.

  Effect on the objective map (nmb3_objective_map.md):
    - O024's Status entry is extended to record the approval
      timestamp, the verbatim approval string, and an explicit
      restatement of the five binding items now in force.
    - No other objective's status changes.  In particular,
      O020 / O021 / O022 remain PROPOSED -- the partial-scope
      approval explicitly preserves the future-expansion path
      to a full-scope v2 recommendation.

  What this approval does NOT do:
    - does NOT advance any later objective (O025+);
    - does NOT generalise the M5.6 baseline beyond the canonical
      6-clip benchmark set;
    - does NOT claim improvement on the M5.6 baseline;
    - does NOT recommend M4 (funding-package-preparation) phase
      transition;
    - does NOT make any funding claim, commercial claim, or
      perceptual-validity claim;
    - does NOT supersede or replace any prior recommendation
      (M3.1 closure recommendation remains in its prior state);
    - does NOT edit any policy file, MILESTONES.md, the M3.1
      archive, the M3.1 closure recommendation, the shared
      reproducibility workflow yml, or any pipeline code;
    - does NOT begin or authorise any new objective execution
      -- the loop will stop after this approval-recording commit
      and propose the next objective without beginning it.

  Loop budget: this approval-recording commit is a bookkeeping
  commit (recording an externally-supplied approval), not a new
  objective execution, so it does not consume an additional loop
  beyond the 6 already used for objective execution in this
  session.  The session is still complete after this commit.

[2026-04-25T17:59:01Z] Loop errata recorded by interpreter (no Steve action required):
  In the loop's report closing O024 (P4 partial-scope closure
  approval), the loop recommended "Execute O033" as a clean
  one-shared-workflow-dispatch follow-on objective.  That
  recommendation did NOT trace O033's declared dependency chain
  (O033 -> O032 -> O031 -> O030 -> O029, where O029 is the
  non-bypassable Steve-gate for P6 entry, all PROPOSED on
  canonical at the time).  When Steve replied "Execute O033" at
  the same timestamp, the loop's pre-execution dependency check
  caught the mismatch: executing O033 directly would have
  violated four levels of declared dependencies, including a
  Steve-gate (O029) with Auto-execute = NO and Forbidden actions
  = "starting without the string".  The loop stopped before
  any dispatch, surfaced the dependency chain to Steve, recorded
  the prior incorrect recommendation as errata, and presented
  five clean string options (A authorise the chain via O029
  entry; B explicit single-objective waiver; C standalone O029;
  D pick a different unblocked objective; E pause).  This is the
  second recommendation-level error caught in this multi-session
  run (the first was the O020 misdescription in the prior
  session).  Same root cause both times: the loop conflated
  "easy to dispatch" with "satisfies declared dependencies".

  No work was performed under the misdescribed authorisation.
  Steve's subsequent gating string ("Override deps for O033
  (single-objective waiver)") was issued against the corrected
  landscape with the chain made explicit.

[2026-04-25T18:00:00Z] Steve, via chat reply to the dependency-chain landscape:
  "Override deps for O033 (single-objective waiver)"

  This is the explicit policy waiver requested as Option B in
  the loop's prior response.

  Effect on the dependency chain (per nmb3/nmb3_objective_map.md):
    - O033's "Dependencies: O032" clause is WAIVED for this
      single execution.  O033 is authorised to execute alone
      against the current canonical M5.6 extractor without first
      re-validating M6.1 (O030) / M6.2 (O031) / M6.3 (O032).
    - O029 (P6 entry Steve-gate) is NOT opened by this waiver.
      O029's status remains PROPOSED.  The waiver authorises
      O033 only -- it does not authorise broader P6 entry.
    - O030 / O031 / O032 statuses are unchanged (PROPOSED).  They
      remain available for future authorisation by Steve and
      would, if executed, give the formal M6.1 / M6.2 / M6.3
      re-validation that this O033 reproduction implies but does
      not formally provide.

  Effect on O033's spec:
    - The "Dependencies: O032" clause is the only clause waived.
      All other clauses bind in full -- in particular:
      * "Forbidden actions: removing the weak-evidence flag;
        relaxing cond4" remains binding.
      * "Success criteria: full table reproduces; weak-evidence
        flag for twinkle_people reproduces; the cond4 honesty
        assertion fires" remains binding.
      * "Evidence required: Block 022 plan + CI bot output +
        interpreter output; explicit pointer to the 5/5 pre-set
        conditions PASS" remains binding.
    - The waiver is for this single execution only.  Future
      O033 re-runs would need either the formal upstream chain
      to be satisfied (preferred) or another explicit waiver.

  Loop interpretation:
    - Execute O033 per its spec (with the waived Dependencies
      clause).  Use the shared no-cache reproducibility workflow
      with target=m6_4 and script="python run_m6_4_family_retrieval.py".
    - Write the Block 022 plan documenting the waiver and the
      execution scope.  Pin the CI run id by SHA in the
      interpreter output.
    - Per the Block 022 no-cherry-picking clause, the first
      dispatch result is the reproduction unless dispatch failed
      for infrastructure reasons.  If a retry is required for an
      infrastructure reason, log the failure honestly and make
      the retry's reasoning explicit (see the 2026-04-25T18:18Z
      entry below).
    - Per the Session Completion Rule, stop after success and
      propose the next objective without beginning it.

[2026-04-25T18:18:54Z] Infrastructure failure + retry, honest record:
  First M6.4 dispatch (run 24937425251 at SHA 59fad71) failed
  with audioread.exceptions.NoBackendError when loading
  twinkle_people.m4a.  Root cause: ubuntu-latest runner lacked
  ffmpeg; libsndfile cannot decode M4A natively; audioread fell
  back without a usable backend.

  Per the Block 022 plan's "Forbidden Actions / no cherry-
  picking" clause: "the first dispatch's result is the
  reproduction unless dispatch failed for infrastructure reasons
  (in which case the failure must be logged honestly and the
  retry's reasoning made explicit in decisions.log)".  This
  entry constitutes that explicit honest log.

  Fix: shared workflow yml extended at canonical commit 3929fbc
  to install ffmpeg as target-agnostic system infrastructure.
  Adding a system audio backend cannot change the numerical
  output of any extractor that successfully decoded a sample, so
  the M5.6 reproduction (Run A under O023 at SHA 1368524,
  107/144 = 0.7431 with all 18 cells bit-identical) remains
  valid; this fix only enables decoding of the .m4a sample that
  M6.4 needs.

  Re-dispatch (run 24937477484 at SHA 3929fbc) succeeded with
  full M6.4 PASS reproducing every cell of the MILESTONES.md
  baseline and cond4 honesty assertion firing PASS.  This is the
  reproduction of record for O033.

  Loop budget cost: 1 of 2 workflow edits used (the ffmpeg
  install) -- a target-agnostic fix that benefits any future
  objective using compressed audio.  The other slot remains
  available for any follow-up infrastructure issue.

[2026-04-25T18:30:00Z] Steve, via chat reply to the O037 dependency-chain
landscape:
  "Execute O034"

  Effect:
    - Authorises NMB3 objective O034 (M6.5 re-validate the Lamb
      FAIL as an honest FAIL).  Declared dependency O033 is
      SATISFIED at canonical SHA 9eaabac (SUCCEEDED at
      2026-04-25T18:21:00Z); no waiver required.
    - This is option E from the loop's preceding O037 surface
      message ("Pick a different unblocked objective; the
      cleanest unblocked single-step is O034").
    - Does NOT advance O037, O036, or O035.  Each remains
      PROPOSED.  Does NOT authorise any broader CI-pinning
      campaign for M3.x / M4.1 / M5.1-M5.5 / M6.1 / M6.2 / M6.3.
      Does NOT re-scope O037 (no Rewrite was issued).

  O034's "Auto-execute permission" clause includes an explicit
  honesty floor: any reproduction passing M6.5 must trigger the
  Decision Policy "suspected metric gaming" early-stop and be
  reported to Steve before any further action.  Per O034's
  forbidden-action clause, no per-family tuning, scoring change,
  extractor edit, comparator downgrade, or cond5 restatement is
  permitted.

  Loop interpretation:
    - Execute O034 per its spec.  Use the shared no-cache
      reproducibility workflow with target=m6_5 and
      script="python run_m6_5_multi_family.py".
    - Write Block 023 plan documenting the scope, the honesty
      floor, and the per-condition pass criteria.
    - On verdict = FAIL with bit-identical arithmetic: standard
      SUCCEEDED workflow.
    - On verdict = FAIL but with cell drift: write findings
      honestly, surface to Steve with the FAIL/drift mixed
      verdict.
    - On verdict = PASS: STOP IMMEDIATELY before any commit;
      surface the anomaly to Steve.
    - Per the Session Completion Rule, stop after success and
      propose the next objective without beginning it.

  Loop budget: 3 of 6 loops used after this objective (counting
  generously: loop 1 = O033 dep-chain catch + Block 022 + ffmpeg
  + 2 CI attempts + O033 SUCCEEDED; loop 2 = O033 errata fixup;
  loop 3 = O037 surface + O034 Block 023 + 1 CI attempt + O034
  SUCCEEDED).  Workflow edits unchanged: 1 of 2 used (the
  ffmpeg install from O033).

[2026-04-25T18:45:00Z] Honesty floor NON-TRIGGER for O034 / M6.5:
  CI run 24937928571 at SHA 9eaabac produced verdict
  "M6.5 FAIL" with 4 of 7 pre-set conditions FAILing (cond2,
  cond3, cond4, cond5) and 3 of 7 PASSing (cond1, cond6, cond7),
  bit-identical to the locked baseline in MILESTONES.md.

  All three trigger conditions for the Decision Policy
  "suspected metric gaming" early-stop were checked and NONE
  fired:
    - The script's final summary line is "M6.5 FAIL" (not PASS).
    - Only 3 of 7 conditions reported PASS (less than the 4
      that would constitute a flip).
    - Both family-level booleans ("Twinkle family", "Lamb
      family") reported FAIL.

  Therefore the loop continued with the standard SUCCEEDED
  workflow.  Recording this honesty-floor non-trigger explicitly
  so the audit trail captures the check at the boundary, not
  only on a hypothetical fire.

[2026-04-25T18:55:00Z] Steve, via chat reply to the O034 SUCCEEDED
surface message:
  "Execute O035"

  Effect:
    - Authorises NMB3 objective O035 (Boundary documentation
      file).  Declared dependency O034 is SATISFIED at canonical
      SHA cf165f4 / 5190b05 (SUCCEEDED at 2026-04-25T18:45:00Z);
      no waiver required.
    - This is option 1 from the loop's preceding O034 SUCCEEDED
      surface message ("O035 -- declared dependency O034 is now
      SATISFIED.  Documentation-only ('write; commit; push'),
      no fresh CI runs.  Single clean unblocked step").
    - Does NOT advance O036, O037, or any P9 (continuation-
      scope) objective.  Does NOT authorise any broader CI-
      pinning campaign.  Does NOT re-scope O037.

  O035's Allowed autonomous actions are documentation-only
  (write; commit; push) and Auto-execute permission is YES
  (documentation only).  Forbidden actions: softening boundary
  language; suggesting out-of-scope fixes (CREPE, voice-friendly
  extractor) anywhere except the explicit "out of scope"
  register (O043).  The CREPE mention that already appears in
  the verbatim MILESTONES.md "Current boundary" passage is
  permitted (because the success criterion requires byte-
  equality).  The forbidden action is *adding* new fix-
  recommendation language outside that verbatim quote.

  Loop interpretation:
    - Write nmb3/nmb3_blocks/block_024_plan.md documenting the
      scope, the verbatim-quote requirement, and the no-
      softening / no-new-fix-language discipline.
    - Read MILESTONES.md "Current boundary" section bytes
      (lines 40-53 at canonical SHA cf165f4) and pin their
      SHA256 (a6fc0d62b760733e96eb40a798fd5c67374bb2689273fcdf42c68d92d21e0cfc).
    - Write nmb3/nmb3_reports/boundary.md with the verbatim
      passage embedded as a fenced quote, the SHA256 pinning,
      the verifier reproduction command, evidence pointers to
      O033 and O034, the prior-NEGATIVE / ABANDONED front-end
      ablations (M5.7, M5.8, M5.9) as factual pointers, and
      the explicit non-claim register.
    - Update objective map O035 PROPOSED -> SUCCEEDED.
    - Per the Session Completion Rule, stop after success and
      propose the next objective without beginning it.

  Loop budget: 4 of 6 loops used after this objective (loop 1
  = O033 main; loop 2 = O033 errata; loop 3 = O034 main; loop
  4 = O034 errata + O035).  Workflow edits unchanged: 1 of 2
  used (still the ffmpeg install from O033; O035 is
  documentation only and needs no workflow edit).
